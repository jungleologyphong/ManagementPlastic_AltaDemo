import React, { useState, useEffect } from 'react';
import { View, Text, FlatList } from 'react-native';

export const MaterialScreen: React.FC<any> = (props: any) => {
  const {} = props;
  return (
    <View>
      <Text>Hi</Text>
    </View>
  ) 
}